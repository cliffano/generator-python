---
name: Release Major
on: [workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: cliffano/release-action@v1.4.0
        with:
          release_type: 'major'
          github_token: $\{{ secrets.{{github_token_prefix}}_GITHUB_TOKEN }}
